/** Vintage letters + pocket watch — branding inspiration (public folder). */
export const BRAND_HEADER_IMAGE = '/matuska-hour-s-1994980.jpg';

/** Photo + warm overlay surface (no positioning). */
export const brandHeaderSurface =
  'notie-brand-header border-b border-border/80 backdrop-blur-[2px]';

/** Sticky app chrome header (Dashboard). */
export const brandHeaderClass = `sticky top-0 z-20 ${brandHeaderSurface}`;
